extends ../layout

block content
  include ../layout/header

  block content
    section.login-content
      .login-box
        div.form-group
          form.login-form(method="post", id="loginForm")
            block.form-group
              label Usuario
              input.form-control(id="Username", type="text", name="Username", placeholder="Ingrese Usuario",required)
            block.form-group
              label Password
              input.form-control(id="Password", type="password", name="Password", placeholder="Ingrese Contraseña", required)
            p
            block.form-group
              input.btn.btn-success.btn-block(type="submit", value="Ingresar")

  //include ../footer